<%= form_for(@discovery_session) do |f| %>
  <% if @discovery_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discovery_session.errors.count, "error") %> prohibited this discovery_session from being saved:</h2>

      <ul>
      <% @discovery_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.number_field :user %>
  </div>
  <div class="field">
    <%= f.label :dimands_session %><br />
    <%= f.number_field :dimands_session %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
